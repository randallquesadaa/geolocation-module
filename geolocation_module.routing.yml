geolocation_module.geolocation_config:
  path: '/admin/elsa-config/geolocation-form'
  defaults:
    _form: '\Drupal\geolocation_module\Form\GeolocationForm'
    _title: 'Geolocation Form'
  requirements:
    _permission: 'custom elsa configuration'
  options:
    _admin_route: TRUE
